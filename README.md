# Static-Default-Routing
GNS3-based network simulation demonstrating Static and Default Routing using Cisco routers. The project covers IP addressing, subnetting, static route configuration, default route configuration, routing table verification, end-to-end connectivity testing, ping, traceroute, and basic network troubleshooting.
